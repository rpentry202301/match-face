export const metadata = {
  title: "質問・解答例作成",
  description: "管理者質問・解答例作成ページ",
};

export default function CreateLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
