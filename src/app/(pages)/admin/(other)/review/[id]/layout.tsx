export const metadata = {
    title: "回答レビュー",
    description: "管理者回答レビューページ",
  };
  
  export default function ReviewLayout({
    children,
  }: {
    children: React.ReactNode;
  }) {
    return <>{children}</>;
  }
  